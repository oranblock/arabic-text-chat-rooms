# Copy to .env and fill in. deploy.sh loads this into the systemd unit.
PORT=3000
DB_FILE=/opt/alichat/server/data/db.json

# Firebase Cloud Messaging (optional). Point to the service-account JSON you
# download from Firebase Console -> Project settings -> Service accounts.
# Leave empty to run without push notifications.
FCM_SERVICE_ACCOUNT=
